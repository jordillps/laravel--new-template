Crea el projecte Laravel

.env

Canviar a mysql. Incloure les dades de la base de dades

Incloure idiomes "es"

composer update (no es necessari)

npm install

php artisan key:generate

php artisan migrate

-------------------------------------------------------------------------------

DEFINIR MODEL USER

Nous atributs al model USER

Usuari Creat (DatabaseSeeder)
Jordi Llobet
jordillps@gmail.com
12345678

php asrtisan migrate:fresh --seed


----------------------------------------------------------------------------------

INSTALACIO FILAMENT 4

composer require filament/filament:"^4.0"

php artisan filament:install --panels

admin

Al Model Usuari:

use Filament\Panel;

implements FilamentUser

//Poden acceder al panell complet de Filament  todos los usuarios  
    public function canAccessPanel(Panel $panel): bool
    {
        return true;
    }


-------------------------------------------------------------------------------------------

MULTILANGUAGE

php artisan lang:publish

php artisan vendor:publish --tag=filament-panels-translations

Eliminar les idiomes no necessaris

Actualitzar els arxius del català

-----------------------------------------------------------------------------------------

CREAR EL RECURS USER

php artisan make:filament-resource User --generate

--generate: gerenar les columns des de la base de dades (normalment ho pregunta i posem yes)

Afegir strings a UserResource

Afegir traduccions

Modificar camps a UsersTable, UserForm, UserInfoList

Recorda: .env http://127.0.0.1:8000 per visualitzar les imatges

-------------------------------------------------------------------------------------------

Añadir profile, emailverification AdminPanelProvider

->profile()
->registration()
->emailVerification()

Configuració mailtrap a .env

Per poder rebre els correus

# QUEUE_CONNECTION=database
QUEUE_CONNECTION=sync

Al Model  User incloure implements MustVerifyEmail

--------------------------------------------------------------------------------

IMPLEMENTACIÓN EMAIL VERIFICACIÓN MULTIIDIOMA - CONFIGURACIÓN FINAL

1. CREAR TRADUCCIONES EN ARCHIVOS DE IDIOMA

En lang/es.json y lang/en.json, agregar las traducciones:

```json
{
    "Verify your email address": "Verifica tu dirección de correo electrónico",
    "Hello :name!": "¡Hola :name!",
    "Thanks for registering with :app_name.": "Gracias por registrarte en :app_name.",
    "Please click the button below to verify your email address.": "Por favor, haz clic en el botón de abajo para verificar tu dirección de correo electrónico.",
    "Verify Email Address": "Verificar dirección de correo",
    "This verification link will expire in :expire minutes.": "Este enlace de verificación expirará en :expire minutos.",
    "If you did not create an account, no further action is required.": "Si no creaste una cuenta, no es necesario realizar ninguna acción.",
    "Regards": "Saludos",
    "The :app_name Team": "El equipo de :app_name"
}
```

2. CREAR CLASE PERSONALIZADA MULTIIDIOMA

Crear app/Notifications/VerifyEmail.php:

```php
<?php

namespace App\Notifications;

use Illuminate\Auth\Notifications\VerifyEmail as BaseVerifyEmail;
use Illuminate\Notifications\Messages\MailMessage;

class VerifyEmail extends BaseVerifyEmail
{
    public function toMail($notifiable)
    {
        $verificationUrl = $this->verificationUrl($notifiable);

        return (new MailMessage)
            ->subject(__('Verify your email address'))
            ->greeting(__('Hello :name!', ['name' => $notifiable->name]))
            ->line(__('Thanks for registering with :app_name.', ['app_name' => config('app.name')]))
            ->line(__('Please click the button below to verify your email address.'))
            ->action(__('Verify Email Address'), $verificationUrl)
            ->line(__('This verification link will expire in :expire minutes.', [
                'expire' => config('auth.verification.expire', 60)
            ]))
            ->line(__('If you did not create an account, no further action is required.'))
            ->salutation(__('Regards') . ',')
            ->salutation(__('The :app_name Team', ['app_name' => config('app.name')]));
    }
}
```

3. CONFIGURAR MODEL USER

En app/Models/User.php:

```php
use App\Notifications\VerifyEmail as VerifyEmailNotification;

public function sendEmailVerificationNotification()
{
    $this->notify(new VerifyEmailNotification);
}
```

4. COMANDOS DE PRUEBA DISPONIBLES

```bash
php artisan test:verify-email-notification {email}           # Clase personalizada
php artisan test:multilang-verify-email {email} --locale=es  # Probar en español
php artisan test:multilang-verify-email {email} --locale=en  # Probar en inglés
```

5. CONFIGURACIÓN FINAL

✅ AppServiceProvider.php - Solo contiene configuración de ResetPassword
✅ Clase VerifyEmail personalizada maneja completamente la verificación
✅ No hay configuraciones duplicadas o conflictos
✅ Sistema completamente desacoplado y mantenible

6. VENTAJAS DE LA IMPLEMENTACIÓN MULTIIDIOMA

✅ Soporte para múltiples idiomas automático
✅ Usa el sistema de traducciones de Laravel
✅ Fácil agregar nuevos idiomas (solo crear archivos .json)
✅ Se adapta al locale configurado en APP_LOCALE
✅ Código mantenible y escalable
✅ Compatible con todas las funciones de localización de Laravel

--------------------------------------------------------------------------------

IMPLEMENTACIÓN EMAIL RESTABLECIMIENTO DE CONTRASEÑA MULTIIDIOMA - CONFIGURACIÓN FINAL

1. CREAR TRADUCCIONES EN ARCHIVOS DE IDIOMA

En lang/es.json y lang/en.json, agregar las traducciones para reset password:

```json
{
    "Reset Password": "Restablecer contraseña",
    "You are receiving this email because we received a password reset request for your account.": "Has recibido este correo porque hemos recibido una solicitud de restablecimiento de contraseña para tu cuenta.",
    "Reset Password Button": "Restablecer contraseña",
    "This password reset link will expire in :expire minutes.": "Este enlace de restablecimiento de contraseña expirará en :expire minutos.",
    "If you did not request a password reset, no further action is required.": "Si no solicitaste el restablecimiento de contraseña, no es necesario realizar ninguna acción."
}
```

2. CREAR CLASE PERSONALIZADA MULTIIDIOMA

Crear app/Notifications/ResetPassword.php:

```php
<?php

namespace App\Notifications;

use Illuminate\Auth\Notifications\ResetPassword as BaseResetPassword;
use Illuminate\Notifications\Messages\MailMessage;

class ResetPassword extends BaseResetPassword
{
    public function toMail($notifiable)
    {
        $url = url(route('custom.password.reset', ['token' => $this->token]) . '?email=' . urlencode($notifiable->email));

        return (new MailMessage)
            ->subject(__('Reset Password'))
            ->greeting(__('Hello :name!', ['name' => $notifiable->name]))
            ->line(__('You are receiving this email because we received a password reset request for your account.'))
            ->action(__('Reset Password Button'), $url)
            ->line(__('This password reset link will expire in :expire minutes.', [
                'expire' => config('auth.passwords.'.config('auth.defaults.passwords').'.expire', 60)
            ]))
            ->line(__('If you did not request a password reset, no further action is required.'))
            ->salutation(__('Regards') . ',')
            ->salutation(__('The :app_name Team', ['app_name' => config('app.name')]));
    }
}
```

3. CONFIGURAR MODEL USER

En app/Models/User.php:

```php
use App\Notifications\ResetPassword as ResetPasswordNotification;

public function sendPasswordResetNotification($token)
{
    $this->notify(new ResetPasswordNotification($token));
}
```

4. COMANDOS DE PRUEBA DISPONIBLES

```bash
php artisan test:password-reset {email}                        # Comando original
php artisan test:multilang-reset-password {email} --locale=es  # Probar en español
php artisan test:multilang-reset-password {email} --locale=en  # Probar en inglés
```

5. CONFIGURACIÓN FINAL

✅ AppServiceProvider.php - Completamente limpio (método boot() vacío)
✅ Ambas notificaciones (VerifyEmail y ResetPassword) usan clases personalizadas
✅ Sistema completamente multiidioma y desacoplado
✅ Rutas personalizadas funcionando sin conflictos 403

6. RESULTADO FINAL

✅ Sistema completamente multiidioma para ambas notificaciones
✅ Traducciones centralizadas en archivos .json
✅ Arquitectura limpia sin configuraciones globales
✅ Fácil mantenimiento y escalabilidad
✅ Compatible con cualquier nuevo idioma agregando archivos .json

--------------------------------------------------------------------------------

IMPLEMENTACIÓN AUTO-ELIMINACIÓN DE AVATARES - CONFIGURACIÓN FINAL

1. CONFIGURACIÓN DEL DISCO DE AVATARES

En config/filesystems.php, agregar disco personalizado:

```php
'avatars' => [
    'driver' => 'local',
    'root' => public_path('media/avatars'),
    'url' => env('APP_URL').'/media/avatars',
    'visibility' => 'public',
],
```

2. EVENTOS EN MODEL USER

En app/Models/User.php, agregar método boot() para eventos:

```php
/**
 * Boot del modelo para eventos
 */
protected static function boot()
{
    parent::boot();

    // Cuando se actualiza un usuario
    static::updating(function ($user) {
        // Si el avatar ha cambiado y había un avatar anterior
        if ($user->isDirty('avatar') && $user->getOriginal('avatar')) {
            $oldAvatar = $user->getOriginal('avatar');
            if (Storage::disk('avatars')->exists($oldAvatar)) {
                Storage::disk('avatars')->delete($oldAvatar);
            }
        }
    });

    // Cuando se elimina un usuario
    static::deleting(function ($user) {
        if ($user->avatar && Storage::disk('avatars')->exists($user->avatar)) {
            Storage::disk('avatars')->delete($user->avatar);
        }
    });
}
```

Importante: Agregar use Illuminate\Support\Facades\Storage; al inicio del archivo.

3. RESULTADO FINAL

✅ Auto-eliminación cuando se actualiza avatar
✅ Auto-eliminación cuando se elimina usuario
✅ Verificación de existencia antes de eliminar
✅ Compatible con Filament FileUpload
✅ No requiere configuración adicional en formularios







